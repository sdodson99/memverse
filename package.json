{
  "name": "root",
  "private": true,
  "scripts": {
    "install": "lerna exec -- npm install",
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "fix": "lerna run fix"
  },
  "devDependencies": {
    "jest": "^27.4.5",
    "jest-when": "^3.5.0",
    "lerna": "^4.0.0"
  }
}

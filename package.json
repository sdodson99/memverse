{
  "name": "root",
  "private": true,
  "scripts": {
    "install": "lerna bootstrap",
    "dev": "lerna run dev",
    "test": "lerna run test"
  },
  "devDependencies": {
    "jest": "^27.4.5",
    "jest-when": "^3.5.0",
    "lerna": "^4.0.0"
  }
}
